const a = 7 // atribuicao
let b = 3

b += a //atribuicao aditiva -> b = b + a

b -= a // atribuicao subtrativa -> b = b - a

b *= a // b = b * a

b /= a // b = b / a

b %= a // b = b % a

console.log(b)